int main(){

decl
	int a,b,temp;
enddecl
begin
	a=0;
	while(a<100) do
		//if(a==50) then
		//	a=a/0;
		//endif;
		a=a+1;
		b=a%2;
		if(b==0) then
			continue;
		else
			temp=exposcall("Write",-2,a);
		endif;
	endwhile;
	//temp=exposcall("Exec","even.xsm");
	return 0;
end
}
